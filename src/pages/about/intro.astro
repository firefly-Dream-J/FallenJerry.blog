---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { Icon } from 'astro-icon/components';
---

<BaseLayout title="自我介绍">
  <div class="page-wrapper">
    
    <div class="glass-card top-section fade-in-up">
      <div class="avatar-wrapper">
        <img src="/avatar.jpg" alt="Jerry" class="avatar" />
      </div>
      <div class="main-info">
        <h1>Jerry</h1>
        <div class="tags">
          <span class="tag">电子科技大学</span>
          <span class="tag">软件工程</span>
          <span class="tag">大二</span>
          <span class="tag">网络安全</span>
        </div>
        <p class="bio">“我从来都不觉得写代码和学数学开心过”</p>
      </div>
    </div>

    <div class="grid-container">
      
      <div class="glass-card info-card fade-in-up delay-1">
        <div class="card-icon" style="background: rgba(253, 230, 138, 0.2); color: #FDE68A;">
          <Icon name="lucide:sun" />
        </div>
        <h3>日常</h3>
        <div class="card-content">
          <p>☀️ 阳光热情，热爱生活</p>
          <p>🥘 美食家 / 懒得出门</p>
          <p>✨ 追求美好的事物</p>
          <p>💡 有很强的分享欲和创作欲</p>
        </div>
      </div>

      <div class="glass-card info-card fade-in-up delay-2">
        <div class="card-icon" style="background: rgba(94, 234, 212, 0.2); color: #5EEAD4;">
          <Icon name="lucide:cloud" />
        </div>
        <h3>幻想</h3>
        <div class="card-content poem-style">
          <p>行于江湖之上</p>
          <p>隐于闹市之间</p>
          <p>入山林不问世事</p>
          <p>不求留名于史册</p>
          <p>但愿平世间纷端</p>
        </div>
      </div>

      <div class="glass-card info-card fade-in-up delay-3">
        <div class="card-icon" style="background: rgba(244, 114, 182, 0.2); color: #F472B6;">
          <Icon name="lucide:bike" />
        </div>
        <h3>偶尔</h3>
        <div class="card-content">
          <p>🚀 想干就干，说走就走</p>
          <p>🌌 喜欢幻想并沉浸其中</p>
          <p>🚶 经常一个人连续几个小时骑车或走路</p>
        </div>
      </div>

    </div>

    <div class="footer-action fade-in-up delay-3">
      <a href="/about" class="back-btn">
        <Icon name="lucide:arrow-left" /> 返回
      </a>
    </div>

  </div>
</BaseLayout>

<style>
  /* --- 页面整体布局 --- */
  .page-wrapper {
    max-width: 1000px; /* 限制最大宽度，防止在大屏上太散 */
    margin: 0 auto;
    padding: 60px 20px;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    gap: 30px;
  }

  /* --- 通用毛玻璃卡片 --- */
  .glass-card {
    background: rgba(15, 23, 42, 0.6); /* 深色半透明 */
    backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 20px;
    padding: 30px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    transition: transform 0.3s ease, border-color 0.3s ease;
  }

  .glass-card:hover {
    transform: translateY(-5px);
    border-color: rgba(255, 255, 255, 0.2);
  }

  /* --- 1. 顶部区域样式 --- */
  .top-section {
    display: flex;
    align-items: center;
    gap: 40px;
    text-align: left;
  }

  .avatar {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    border: 4px solid rgba(255, 255, 255, 0.1);
    object-fit: cover;
    box-shadow: 0 0 20px rgba(94, 234, 212, 0.3); /* 青色光晕 */
  }

  .main-info h1 {
    font-size: 2.5rem;
    color: #fff;
    margin: 0 0 15px 0;
  }

  .tags {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    margin-bottom: 15px;
  }

  .tag {
    background: rgba(94, 234, 212, 0.1);
    color: var(--accent); /* 青色 */
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 0.9rem;
    border: 1px solid rgba(94, 234, 212, 0.2);
  }

  .bio {
    color: #cbd5e1;
    font-size: 1.1rem;
    line-height: 1.6;
    margin: 0;
  }

  /* --- 2. 底部 Grid 布局 (核心还原草图) --- */
  .grid-container {
    display: grid;
    /* 核心：创建3列，每列等宽 */
    grid-template-columns: 1fr 1fr 1fr; 
    gap: 25px;
  }

  .info-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  /* 卡片顶部图标 */
  .card-icon {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    margin-bottom: 15px;
  }

  .info-card h3 {
    color: #fff;
    font-size: 1.3rem;
    margin: 0 0 20px 0;
    font-weight: bold;
  }

  .card-content {
    color: #94a3b8;
    font-size: 0.95rem;
    line-height: 1.8;
  }
  
  .card-content p {
    margin: 5px 0;
  }

  /* "幻想"部分的诗歌样式 */
  .poem-style {
    font-family: "KaiTi", "STKaiti", serif; /* 尝试使用楷体，更有江湖气 */
    font-size: 1.05rem;
    color: #e2e8f0;
    font-style: italic;
  }

  /* --- 底部按钮 --- */
  .footer-action {
    display: flex;
    justify-content: center;
    margin-top: 20px;
  }

  .back-btn {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 10px 30px;
    border: 1px solid var(--accent);
    border-radius: 50px;
    color: var(--accent);
    text-decoration: none;
    transition: 0.3s;
    background: rgba(15, 23, 42, 0.8);
  }

  .back-btn:hover {
    background: var(--accent);
    color: #0F172A;
  }

  /* --- 动画 --- */
  .fade-in-up { opacity: 0; transform: translateY(20px); animation: fadeInUp 0.8s forwards; }
  .delay-1 { animation-delay: 0.2s; }
  .delay-2 { animation-delay: 0.4s; }
  .delay-3 { animation-delay: 0.6s; }
  @keyframes fadeInUp { to { opacity: 1; transform: translateY(0); } }

  /* --- 移动端适配 --- */
  @media (max-width: 768px) {
    /* 顶部改为垂直排列 */
    .top-section {
      flex-direction: column;
      text-align: center;
      gap: 20px;
    }
    .tags { justify-content: center; }
    
    /* 底部 Grid 改为单列 */
    .grid-container {
      grid-template-columns: 1fr;
    }
  }
</style>