---
const today = new Date();
---


<style>
	footer {
		padding: 4em 1em;
		background: transparent;
		color: var(--gray);
		text-align: center;
        font-size: 0.9em;
        margin-top: auto;
	}
    
    .copyright { margin-bottom: 0.5em; }
    .author { color: var(--accent); font-weight: bold; }
    
    .powered-by { font-size: 0.8em; opacity: 0.7; }
    .powered-by a { color: var(--gray); text-decoration: underline; }
    .powered-by a:hover { color: var(--accent); }
</style>