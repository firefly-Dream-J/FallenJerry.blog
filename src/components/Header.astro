---
import HeaderLink from './HeaderLink.astro';
import { SITE_TITLE } from '../consts';
// 引入图标组件
import { Icon } from 'astro-icon/components';
---

<header id="main-header">
  <nav>
    <div class="header-left">
      <a href="/" class="mini-home" aria-label="回到首页">
        <Icon name="lucide:sparkles" class="logo-icon"/>
      </a>
    </div>

    <div class="internal-links">
      <HeaderLink href="/">
        <Icon name="lucide:home" class="nav-icon" /> 首页
      </HeaderLink>
      
      <HeaderLink href="/archive">
        <Icon name="lucide:archive" class="nav-icon" /> 归档
      </HeaderLink>
      
      <HeaderLink href="/tags">
        <Icon name="lucide:tags" class="nav-icon" /> 标签
      </HeaderLink>
      
      <HeaderLink href="/about">
        <Icon name="lucide:user" class="nav-icon" /> 关于
      </HeaderLink>
    </div>

    <div class="user-profile">
      <img src="/avatar.jpg" alt="Firefly" class="avatar" />
      
      <div class="dropdown-menu">
        <p class="user-id">ID: Jerry</p>
        <hr/>
        <a href="/friends" class="friend-link">
          <Icon name="lucide:link" class="dropdown-icon" /> 友情链接
        </a>
      </div>
    </div>
  </nav>
</header>

<style>
  /* --- 1. Header 基础样式 (固定且透明) --- */
  header {
    margin: 0;
    padding: 0 1em;
    
    /* 核心：初始背景完全透明，让壁纸透出来 */
    background: transparent; 
    
    border-bottom: none;
    position: fixed; /* 核心：固定在顶部 */
    top: 0;
    left: 0;
    width: 100%;
    z-index: 100; /* 保证在壁纸之上 */
    transition: all 0.4s ease-in-out; /* 平滑过渡动画 */
  }

  /* --- 2. 滚动后的样式 (由 JS 添加) --- */
  header.scrolled {
    /* 滚动后变成半透明深色毛玻璃，适配流萤夜空主题 */
    background: rgba(15, 23, 42, 0.85); 
    backdrop-filter: blur(12px); /* 毛玻璃特效 */
    -webkit-backdrop-filter: blur(12px);
    box-shadow: 0 4px 20px rgba(0,0,0,0.3); 
    padding: 0.2em 1em; /* 稍微压缩高度，显得更精致 */
  }

  nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1em 0;
    max-width: 1200px;
    margin: auto;
  }

  /* --- 3. 导航链接容器 --- */
  .internal-links {
    display: flex;
    gap: 1.5em;
    /* 初始状态不需要背景，直接悬浮在壁纸上 */
    transition: all 0.3s;
  }

  /* 图标微调 */
  .nav-icon {
    width: 1.1em;
    height: 1.1em;
    margin-right: 4px;
    vertical-align: text-bottom;
  }

  /* 左侧 Logo 图标 */
  .logo-icon {
    width: 1.8em;
    height: 1.8em;
    color: var(--accent); /* 使用青色 */
    filter: drop-shadow(0 0 5px rgba(94, 234, 212, 0.5)); /* 发光效果 */
    transition: transform 0.5s;
  }
  .mini-home:hover .logo-icon {
    transform: rotate(180deg) scale(1.1);
  }

  /* --- 4. 头像与下拉菜单 (核心修复部分) --- */
  .user-profile {
    position: relative;
    display: flex;
    align-items: center;
    cursor: pointer;
    /* 关键：增加 padding，扩大鼠标感应区域，防止误触消失 */
    padding: 10px 0; 
  }

  .avatar {
    width: 42px;
    height: 42px;
    border-radius: 50%;
    object-fit: cover;
    border: 2px solid var(--accent); /* 青色边框 */
    transition: transform 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
    box-shadow: 0 0 10px rgba(94, 234, 212, 0.3);
  }

  .user-profile:hover .avatar {
    transform: scale(1.1);
    box-shadow: 0 0 15px rgba(94, 234, 212, 0.6);
  }

  /* 下拉菜单 */
  .dropdown-menu {
    display: none; /* 默认隐藏 */
    position: absolute;
    /* 让菜单紧贴头像下方 */
    top: 100%;
    right: 0;
    
    /* 配色：深蓝背景，适配主题 */
    background: rgba(15, 23, 42, 0.95); 
    border: 1px solid rgba(255, 255, 255, 0.1); /* 微微发光的边框 */
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
    color: white;
    
    border-radius: 12px;
    padding: 15px;
    min-width: 150px;
    text-align: center;
    
    /* 动画 */
    animation: fadeIn 0.2s ease-out;
    z-index: 101;
  }

  /* 🛠️ Bug 修复：添加透明桥梁 */
  /* 这块看不见的区域连接了头像和菜单，鼠标经过时菜单不会消失 */
  .dropdown-menu::before {
    content: '';
    position: absolute;
    top: -20px; /* 向上延伸覆盖空隙 */
    left: 0;
    width: 100%;
    height: 20px;
    background: transparent;
  }

  /* 悬停显示 */
  .user-profile:hover .dropdown-menu {
    display: block;
  }

  /* 菜单内文字样式 */
  .user-id {
    margin: 0 0 10px 0;
    font-weight: bold;
    color: var(--accent); /* 青色 ID */
    font-size: 1em;
    letter-spacing: 0.5px;
  }

  /* 友链按钮 */
  .friend-link {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    text-decoration: none;
    font-size: 0.9em;
    
    color: #fff;
    background: rgba(255, 255, 255, 0.1); /* 按钮背景 */
    padding: 8px 12px;
    border-radius: 8px;
    transition: all 0.2s;
  }
  
  .friend-link:hover {
    background: var(--accent); /* 悬停变青色 */
    color: #0F172A; /* 文字变深色 */
    transform: translateY(-2px);
  }
  
  .dropdown-icon {
    width: 1em;
    height: 1em;
  }

  hr {
    margin: 10px 0;
    border-top: 1px dashed rgba(255, 255, 255, 0.2);
  }

  /* 淡入动画 */
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* --- 移动端适配 --- */
  @media (max-width: 720px) {
    nav { padding: 1em; }
    /* 手机端暂时隐藏文字导航，以免拥挤 */
    .internal-links { display: none; } 
  }
</style>

<script>
  // 监听页面滚动，切换 Header 样式
  const header = document.getElementById('main-header');
  
  const handleScroll = () => {
    if (!header) return;
    
    // 当滚动超过 50px 时，添加 .scrolled 类（变深色背景）
    // 否则移除（变透明）
    if (window.scrollY > 50) {
      header.classList.add('scrolled');
    } else {
      header.classList.remove('scrolled');
    }
  };
  
  window.addEventListener('scroll', handleScroll);
  // 初始化执行一次
  handleScroll();
</script>